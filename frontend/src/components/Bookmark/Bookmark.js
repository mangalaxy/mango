import React from 'react';
import './Bookmark.scss';

const Bookmark = ({click, title}) => {
  return(
    <>        
      <div onClick={click} className="" title={title}>
        <img src={} alt=""/>
      </div>
    </>
  )
}
export default Remove;